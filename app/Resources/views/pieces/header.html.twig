<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">States Machines & Worfklow Nets in a Videogame</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <p class="navbar-text">{{ player_character.name }}{{ ' - ' }}{{ player_character.mapZone.name }}</p>
                </li>
                <li role="separator" class="divider"></li>
                <li>
                    <p class="navbar-text">{{ player_character.health }}</p>
                    <a class="navbar-brand" href="#">
                        <img width="22px" src="{{ asset('images/health.png') }}">
                    </a>
                </li>
                <li role="separator" class="divider"></li>
                <li>
                    <p class="navbar-text">{{ player_character.attack }}</p>
                    <a class="navbar-brand" href="#">
                        <img width="22px" src="{{ asset('images/attack_attr.png') }}">
                    </a>
                </li>
                <li role="separator" class="divider"></li>
                <li>
                    <p class="navbar-text">{{ player_character.defense }}</p>
                    <a class="navbar-brand" href="#">
                        <img width="22px" src="{{ asset('images/defense_attr.jpeg') }}">
                    </a>
                </li>
                <li role="separator" class="divider"></li>
                <li>
                    <p class="navbar-text">{{ player_character.gold }}</p>
                    <a class="navbar-brand" href="#">
                        <img width="22px" src="{{ asset('images/gold.png') }}">
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>